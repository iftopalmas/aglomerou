{
  "expo": {
    "name": "app",
    "slug": "app",
    "platforms": [
      "ios",
      "android",
      "web"
    ],
    "version": "1.0.0",
    "orientation": "portrait",
    "icon": "./assets/icon.png",
    "splash": {
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "updates": {
      "fallbackToCacheTimeout": 0
    },
    "assetBundlePatterns": [
      "**/*"
    ],
    "ios": {
      "supportsTablet": true,
      "config": {
        "googleMapsApiKey": "precisa obter a chave a partir de uma variável de ambiente. No mobile foi process.env.REACT_APP_GOOGLE_MAPS_API_KEY, mas era dentro de um arquivo js"
      }
    },
    "android": {
      "config": {
        "googleMaps": {
          "apiKey": "precisa obter a chave a partir de uma variável de ambiente. No mobile foi process.env.REACT_APP_GOOGLE_MAPS_API_KEY, mas era dentro de um arquivo js"
        }
      }
    }
  }
}
